const { messageEmbed } = require('discord.js');

module.exports = {
  info: {
    name: "serverinfo",
    description: "to get server information",
    usage: "[serverinfo]",
    aliases: ["svi"],
  }}

messageEmbed.setColor()