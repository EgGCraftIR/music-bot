const { MessageEmbed } = require('discord.js')

module.exports = {
  info: {
    name: "comingsoon",
    description: "new command",
    usage: "",
    aliases: "cs"
  },
  
  run: async function ( message, client, args ) {
  
  var premission = 0;
  
  let comingsoon = new messageEmbed()
  .setColor("YELLOW")
  .setDescription("comingsoon")
  .setAuthor('comingsoon')
  .s
 },
};
